{{- if .Values.crds.install -}}
#+kubebuilder:scaffold:crdkustomizeresource
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: breakglasses.access.cloudnimbus.io
  labels:
    {{- include "firedoor.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  group: access.cloudnimbus.io
  names:
    kind: Breakglass
    listKind: BreakglassList
    plural: breakglasses
    singular: breakglass
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        description: Breakglass is the Schema for the breakglasses API
        type: object
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BreakglassSpec defines the desired state of Breakglass
            type: object
            properties:
              accessPolicy:
                description: AccessPolicy defines the access control policy
                type: object
                properties:
                  rules:
                    type: array
                    items:
                      type: object
                      properties:
                        actions:
                          type: array
                          items:
                            type: string
                        resources:
                          type: array
                          items:
                            type: string
                        subjects:
                          type: array
                          items:
                            type: string
              approvalRequired:
                description: ApprovalRequired indicates if approval is required for break glass access
                type: boolean
              duration:
                description: Duration specifies how long the break glass access is valid
                type: string
              justification:
                description: Justification for the break glass access
                type: string
              reason:
                description: Reason for the break glass access
                type: string
          status:
            description: BreakglassStatus defines the observed state of Breakglass
            type: object
            properties:
              approved:
                description: Approved indicates if the break glass access has been approved
                type: boolean
              approvedAt:
                description: ApprovedAt indicates when the break glass access was approved
                type: string
                format: date-time
              approvedBy:
                description: ApprovedBy indicates who approved the break glass access
                type: string
              conditions:
                description: Conditions represents the latest available observations of the break glass access
                type: array
                items:
                  type: object
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition transitioned from one status to another
                      type: string
                      format: date-time
                    message:
                      description: Message is a human readable message indicating details about the transition
                      type: string
                    reason:
                      description: Reason is a unique, one-word, CamelCase reason for the condition's last transition
                      type: string
                    status:
                      description: Status is the status of the condition
                      type: string
                      enum:
                      - "True"
                      - "False"
                      - "Unknown"
                    type:
                      description: Type is the type of the condition
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
              expiresAt:
                description: ExpiresAt indicates when the break glass access expires
                type: string
                format: date-time
              grantedAt:
                description: GrantedAt indicates when the break glass access was granted
                type: string
                format: date-time
              state:
                description: State represents the current state of the break glass access
                type: string
                enum:
                - Pending
                - Approved
                - Rejected
                - Active
                - Expired
                - Revoked
    subresources:
      status: {}
{{- end }} 